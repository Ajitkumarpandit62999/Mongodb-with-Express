<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat Message</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f8ff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .form-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 350px;
            animation: fadeIn 0.5s ease-in-out;
        }
        h2 {
            text-align: center;
            color: #4b6584;
            margin-bottom: 20px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            color: #4b6584;
        }
        input, textarea {
            width: calc(100% - 16px);
            padding: 8px;
            border: 1px solid #dcdde1;
            border-radius: 8px;
            outline: none;
            font-size: 14px;
        }
        input:focus, textarea:focus {
            border-color: #74b9ff;
            box-shadow: 0 0 5px #74b9ff;
        }
        button {
            width: 100%;
            padding: 10px;
            background-color: #54a0ff;
            color: #fff;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 10px;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #3867d6;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
    </style>
</head>
<body>

    <div class="form-container">
        <h2>Edit Chat Message</h2>
        <form action="/<%= chat.id %>?_method=PUT" method="POST">
            <div class="form-group">
                <label for="from">From:</label>
                <input type="text" id="from" name="from" value="<%= chat.from %>" required readonly>
            </div>
            <div class="form-group">
                <label for="to">To:</label>
                <input type="text" id="to" name="to" value="<%= chat.to %>" required readonly>
            </div>
            <div class="form-group">
                <label for="msg">Message:</label>
                <textarea id="msg" name="msg" rows="4" required><%= chat.msg %></textarea>
            </div>
            <div class="form-group">
                <label for="created_at">Created At:</label>
                <input type="datetime-local" id="created_at" name="created_at" value=" <%=chat.created_at %>" required>
            </div>
            <button type="submit">Save Changes</button>
        </form>
    </div>

</body>
</html>
